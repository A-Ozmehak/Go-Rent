import { Button } from "@chakra-ui/react";
import React from "react";

// red button for decline and such. Send text and stuff as props

const RejectButton = () => {
  return <Button variant="deny"></Button>;
};

export default RejectButton;
